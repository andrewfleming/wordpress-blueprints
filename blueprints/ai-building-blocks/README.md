# AI Building Blocks

**Status: stub.** This blueprint currently just boots a plain WordPress site with Gutenberg installed and logs in as admin. Flesh it out with the actual plugins, theme, and content this demo needs.

## Open in Studio / Playground

```
https://playground.wordpress.net/#{minified blueprint.json here}
```

Or with the CLI:

```bash
npx @wp-playground/cli server --blueprint=./blueprints/ai-building-blocks/blueprint.json
```
